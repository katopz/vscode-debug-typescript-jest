{
  "scripts": {
    "dev": "tsc -w",
    "dev-test": "jest --clearCache && jest --watchAll --runInBand --detectOpenHandles --no-cache ",
    "build": "tsc"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4"
  }
}