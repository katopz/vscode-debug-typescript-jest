{
  "scripts": {
    "dev": "tsc -w",
    "test": "jest --clearCache && jest --runInBand --detectOpenHandles --no-cache ",
    "test-dev": "jest --clearCache && jest --watchAll --runInBand --detectOpenHandles --no-cache ",
    "build": "tsc"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4"
  }
}